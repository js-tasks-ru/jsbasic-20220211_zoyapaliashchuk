fuction factorial (n) {
  let n! = n * (n - 1) * (n - 2) * ...*1
  let result  = n!
      for (let n!; n! > 0; n!++) {
        return result; 
      }
 if ((n! > 0) && (n! % 2 == 0) & (n! % 2! == 0 ) && (++n! % 2 == 0) ) {
   alert (result);
 } if (n! == 0 && n! == 1) {
     result (1); 
   }
}
